import { moduleCamelToken } from './module-kebab-token';

describe('module-kebab-token', () => {
  it('should be a function', () => {
    expect(typeof moduleCamelToken).toEqual('function');
  });
});
